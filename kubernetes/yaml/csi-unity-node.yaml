# This YAML defines all API objects to create RBAC roles for CSI node plugin
apiVersion: v1
kind: ServiceAccount
metadata:
  name: csi-unity-node
---
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-unity-node
rules:
  - apiGroups: [""]
    resources: ["nodes"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "list", "watch", "update"]
  - apiGroups: ["storage.k8s.io"]
    resources: ["volumeattachments"]
    verbs: ["get", "list", "watch", "update"]
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: csi-unity-node
subjects:
  - kind: ServiceAccount
    name: csi-unity-node
    namespace: default
roleRef:
  kind: ClusterRole
  name: csi-unity-node
  apiGroup: rbac.authorization.k8s.io
---
kind: DaemonSet
apiVersion: apps/v1beta2
metadata:
  name: csi-unity-node
  namespace: default
spec:
  selector:
    matchLabels:
      app: csi-unity-node
  template:
    metadata:
      labels:
        app: csi-unity-node
    spec:
      serviceAccount: csi-unity-node
      hostNetwork: true
      containers:
        - name: csi-unity-node
          securityContext:
            privileged: true
            capabilities:
              add: ["SYS_ADMIN"]
            allowPrivilegeEscalation: true
          image: docker.io/cduchesne/csi-unity
          env:
            - name: CSI_ENDPOINT
              value: unix:///var/lib/kubelet/plugins/com.thecodeteam.unity/csi.sock
            - name: X_CSI_DEBUG
              value: "true"
            - name: X_CSI_UNITY_AUTOPROBE
              value: "true"
            - name: X_CSI_PRIVATE_MOUNT_DIR
              value: "/var/lib/kubelet/plugins/com.thecodeteam.unity/disks"
          volumeMounts:
            - name: driver-path
              mountPath: /var/lib/kubelet/plugins/com.thecodeteam.unity
              #mountPropagation: "Bidirectional"
            - name: pods-path
              mountPath: /var/lib/kubelet/pods
              mountPropagation: "Bidirectional"
            - name: dev
              mountPath: /dev
            - name: unity-path-opt
              mountPath: /opt/emc
            - name: unity-path-bin
              mountPath: /bin/emc
        - name: driver-registrar
          image: docker.io/k8scsi/driver-registrar
          args:
            - "--v=5"
            - "--csi-address=$(ADDRESS)"
          env:
            - name: ADDRESS
              value: /var/run/csi/csi.sock
            - name: KUBE_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - name: driver-path
              mountPath: /var/run/csi
      volumes:
        - name: driver-path
          hostPath:
            path: /var/lib/kubelet/plugins/com.thecodeteam.unity
            type: DirectoryOrCreate
        - name: pods-path
          hostPath:
            path: /var/lib/kubelet/pods
            type: Directory
        - name: dev
          hostPath:
            path: /dev
            type: Directory
        - name: unity-path-opt
          hostPath:
            path: /opt/emc
            type: Directory
        - name: unity-path-bin
          hostPath:
            path: /bin/emc
            type: Directory
